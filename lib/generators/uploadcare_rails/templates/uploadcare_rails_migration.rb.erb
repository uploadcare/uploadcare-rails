class <%= migration_class_name %> < ActiveRecord::Migration
  def self.up
  <% field_names.each do |field_name, field_type| -%>
  add_column :<%= name.underscore.camelize.tableize %>, :<%= field_name %>, :<%= field_type %>
  <% end -%>
end

  def self.down
  <% field_names.each do |field_name, field_type| -%>
  remove_column :<%= name.underscore.camelize.tableize %>, :<%= field_name %>
  <% end -%>
end
end
