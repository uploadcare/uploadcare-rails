class <%= migration_name %> < ActiveRecord::Migration
  def self.up
	<% fields.each do |name, type| %>
    	add_column :<%= class_name.underscore.camelize.tableize %>, :<%= name %>, :<%= type %>
	<% end %>
  end

  def self.down
	<% fields.keys.each do |name| %>
    	remove_column :<%= class_name.underscore.camelize.tableize %>, :<%= name %>
	<% end %>
  end
end
    